@model AddAuctionViewModel
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <div class="m-4 p-3 border" style="direction:rtl; font-size:x-large;">
        <form class="form-control" method="post">
            <div class="input-group mb-3">
                <label asp-for="OfferSubmitWithPrice"></label>
                <input type="text" asp-for="OfferSubmitWithPrice">
                <span asp-validation-for="OfferSubmitWithPrice"></span>
            </div>
            <div class="input-group mb-3">
                <label asp-for="StartTime"></label>
                <input type="datetime-local" asp-for="StartTime">
                <span asp-validation-for="StartTime"></span>
            </div>
            <div class="input-group mb-3">
                <label asp-for="EndTime"></label>
                <input type="datetime-local" asp-for="EndTime">
                <span asp-validation-for="EndTime"></span>
            </div>
            <div class="input-group mb-3">
                <span asp-validation-for="ProductDtoIds"></span>
                <select asp-for="ProductDtoIds" multiple class="form-select" aria-label="Default select example">

                    @foreach (var item in ViewBag.Products)
                    {


                        @if (item.IsActive)
                        {
                            <option value="@item.Id">@item.ProductName</option>

                        }
                    }

                </select>

            </div>-->
            <div class="input-group mb-3">
                <button type="submit" asp-controller="OrderManagemnet" asp-action="AddAuction" class="btn btn-primary mb-3">ثبت نهایی</button>
            </div>
        </form>

    </div>
